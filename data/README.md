# Data

`demo_news.csv` is a tiny smoke-test dataset so the project can be run immediately.

For meaningful evaluation, replace it with a public labelled dataset.

Recommended choices:
- WELFake: a larger combined fake/real news corpus
- Fake.csv + True.csv: the commonly used Kaggle Fake and Real News dataset

Do not treat the tiny demo dataset's metrics as research results.
